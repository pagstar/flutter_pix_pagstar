name: flutter_pix_pagstar
description: A Flutter package for interacting with the Pagstar Pix API
version: 1.0.1
repository: https://github.com/pagstar/flutter_pix_pagstar

environment:
  sdk: '>=3.0.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  assets:
    - assets/success.png
    - assets/error.png
